import {useEffect,useRef} from 'react';
export default function useModal(onClose){const ref=useRef(null);useEffect(()=>{const old=document.activeElement;const el=ref.current;const selector='button:not([disabled]),a[href],input,select,textarea';el?.querySelector(selector)?.focus();const key=e=>{if(e.key==='Escape')onClose();if(e.key==='Tab'){const list=[...el.querySelectorAll(selector)];const first=list[0],last=list.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last?.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first?.focus();}}};el?.addEventListener('keydown',key);return()=>{el?.removeEventListener('keydown',key);old?.focus();};},[]);return ref;}
